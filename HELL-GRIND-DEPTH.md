# Hell Grind 深度提炼：AI短视频可控生产系统

> 核心洞见：模型没有跨镜头记忆，一致性不是模型自带功能，是你通过「强制重复描述 + 减少模型自由度」人为制造出来的。

## TLDR

Hell Grind 是95分钟全AI生成长片，由15人团队在资产准备后14天内完成，预算不到50万美元。它没有直接写短视频教程，但把所有失败经验沉淀成了一套**可缩放的生产原则**，一个人也能跑。

对短视频最有价值的五条铁律：
1. **资产优先** — 角色/地点锁定测试完再开干
2. **每次完整重复描述** — 模型没有记忆，绝不缩写
3. **一次只改一个地方** — 整段重写会丢掉已经生效的部分
4. **给模型更少自由度** — 用角落代替整房，用地标代替开放空间
5. **10-15次不行就简化镜头** — 不要堆提示词，直接拆镜头减动作

---

## 目录
- [一、核心问题（所有规则的起点）](#一核心问题所有规则的起点)
- [二、五大压缩规则（原项目总结，可直接缩放）](#二五大压缩规则原项目总结可直接缩放)
- [三、前期制作：资产系统](#三前期制作资产系统)
- [四、空间锁定：GEO SPATIAL LAYOUT](#四空间锁定geo-spatial-layout)
- [五、第一秒永远是广角定位置](#五第一秒永远是广角定位置)
- [六、提示词骨架与写作规则](#六提示词骨架与写作规则)
- [七、生产与迭代纪律](#七生产与迭代纪律)
- [八、后期制作原则](#八后期制作原则)
- [九、短视频完整可执行工作流](#九短视频完整可执行工作流)
- [十、常见失败点](#十常见失败点)
- [十一、行动建议](#十一行动建议)

---

## 一、核心问题（所有规则的起点）

AI视频模型**没有跨镜头记忆**。

你这一镜描述不完整，下一镜角色就会换脸、换衣服、换位置、换声音。

一致性不是模型能给你的，是你通过「强制重复描述 + 减少模型自由度」人为制造出来的。

**铁律一句话：Describe everything, every time.**
描述词必须每次原样粘贴，绝不缩写。

---

## 二、五大压缩规则（原项目总结，可直接缩放）

1. **Assets first** — 任何镜头生成前，角色、地点、道具必须全部锁定并完成压力测试。这一条比其他所有技巧加起来都省钱。

2. **Describe everything, every time** — 模型没有记忆。角色描述、地点描述、声音描述必须每次完整重复。

3. **Change one thing at a time** — 每次迭代只改一行。整段重写会丢掉已经生效的部分。所有修改必须进日志。

4. **Give the model less freedom** — 用角落代替整间房，用地标代替开放空间，用地图代替猜测，一镜只做一个主动作。

5. **If a shot will not come together — simplify the shot, not the words** — 10–15次迭代仍不理想，就拆镜、减动作、改角度，而不是继续堆提示词。

这五条规则可直接缩放到一个人做短视频。

---

## 三、前期制作：资产系统（短视频最该完整复制的部分）

资产 = **文本描述（descriptor）+ 参考图**。两者缺一不可。

参考图只用于角色和地点，其他全靠文本。

### 1. 角色表（Character Sheet）—— 三视图 + 正面无头

| 面板 | 内容 | 要求 |
|------|------|------|
| 1 | 脸部特写 | 3/4侧面最佳，有catch-light（瞳孔反光） |
| 2 | 全身正面 | **故意把头去掉** |
| 3 | 全身背面 | 完整 |

**为什么正面无头？**
广角时模型会去抓全身图上那个小而模糊的脸，导致脸部质量崩。去掉头后模型只能从特写取脸。

**其他要点：**
- 故意做成"无聊"的：中性灰背景、平光、真实毛孔、不修图、不加电影感、不带胶片颗粒。电影感留给场景和视频提示词。
- 选"最可信"的脸，而不是"最美"的脸。假美的脸在视频里会立刻露馅。
- **压力测试必须做**：不同姿势、光线、与其他角色同框生成10次，必须全部可识别才锁定。

### 2. 点状修改（衣服、伤疤、血迹、湿身）

- 用图像编辑模型做局部修改，再用蒙版手工贴回原图。
- **绝对禁止让整张图二次完整跑模型**。每多跑一次，纹理就死一次，最后变成塑料对称脸，表演也会跟着死。

### 3. 状态拆分

湿身、受伤、换装、白天/夜晚/雨天，全部做成独立资产（`@girl`、`@girl_rain`、`@girl_blood`）。
同一道具也有多版本（完整版、血污版、握在拳中只露光的版本）。

混在一起写，模型就会在镜头之间乱混。

### 4. 声音锁定

声音不是资产，是固定描述词。提前写好，每次原样粘贴到音频字段：

```
Voice: deep, gravelly bass-baritone; slow, calculated pacing; London street accent; menacing calm — he never raises his voice.
```

声音也要做压力测试，漂移就回去加强锁定。

### 5. 表演习惯锁定

每个角色写一段固定行为描述（走路节奏、手部习惯、压力下的动作、眼睛行为）。场景里只能适配，不能改核心。

物理上做不到的动作要**转移能量**（原本踱步的人坐下后，改成身体晃动 + 手指敲击）。

### 6. 地点表

- 优先用3/4视角（正面图容易变成壁纸，空间信息不足）。
- 每个地点留一个视觉锚点（柱子、灯、沙发）。
- 灯光逻辑统一（一个主光源方向）。
- 反向角度可以用「空镜视频截图 + 再编辑」的方式生成。

### 7. 命名与角色声明

所有资产统一标签（`@roco`、`@loc_street_rain`），全项目、全提示词、全界面只用这一套名字。

地点参考必须明确禁止继承构图/角度/调色：

```
@loc_street_rain for location reference — take only the space and the texture: wet asphalt, neon reflections. Do not use as a starting frame, do not inherit the composition, the angle, or the grade.
```

---

## 四、空间锁定：GEO SPATIAL LAYOUT（短视频最实用的武器）

每场戏写一份**纯空间地图**（不含角色和动作），该场景所有镜头原样粘贴。

示例：

```
GEO SPATIAL LAYOUT (locked across every shot — pure spatial map):
— STREET runs left-right across the frame.
— NEON SIGN on the far wall, frame-RIGHT, about 8 meters from camera.
— WET ASPHALT reflects blue and pink neon.
— SIDEWALK on frame-LEFT, slightly elevated.
— 180° AXIS: camera ALWAYS stays on the sidewalk side — it NEVER crosses into the street center.
— BACK-LIGHTING: neon glow comes from frame-RIGHT, rim-lighting figures from camera's perspective.
```

**规则：**
- 左右一律用 `frame-left` / `frame-right`（模型不懂"角色左边"）。
- 位置用地标 + 米数。
- 明确摄像机永不越轴。
- 静态对话尽量给角落，而不是整间房（自由度越低，位置越稳）。
- 每切一镜，重新声明谁站在哪里、看向哪里。

---

## 五、第一秒永远是广角定位置

每场戏开头约1秒：只定站位、光线、物体，**不做动作**。

模型会"拍照"记住这个布局，并带到后续镜头。

**两个生产中验证过的技巧：**
- 让角色在这1秒里说一声"hm"，模型更容易把这段当独立镜头处理。
- 如果接上一条，可以把上一镜的尾音喂进这1秒，帮助缝合。

---

## 六、提示词骨架与写作规则（短视频精简可用版）

原项目提示词常到3000–4000词，短视频可大幅精简，但**骨架顺序和强制规则必须保留**。

**推荐短视频骨架顺序：**
1. 人物数量头部：`EXACT 1 CHARACTER — NO DUPLICATES: GIRL.`
2. 角色参考声明 + 完整描述
3. 地点参考声明（带禁止继承）
4. GEO SPATIAL LAYOUT
5. SCENE CONTEXT
6. FIRST FRAME AND SPATIAL BLOCKING（第一秒广角）
7. ACTION TIMING（按秒拆分，每节拍最多3句）
8. CHARACTER ACTING（目标 + 障碍 + 身体节奏 + 习惯）
9. STYLE PREFIX（固定前缀）
10. QUALITY + 技术标签（Photoreal. SFX only. No music. 等）

**写作铁律（直接来自原项目）：**
- 现在时 + 短句。
- 每个动作节拍最多3句，过载会糊。
- 只写正面动作（"倒在肚子上"而不是"不要仰倒"）。
- 角色从第一帧就在画面里，不看镜头（除非特意要求）。
- 绝不写年龄（内容过滤会变严）。
- 有禁用词字典（"dark"改成"low key"等）。
- 固定风格前缀每次原样粘贴，末尾强制 `SFX only. No music.`（音乐属于后期）。

**表演写法核心：** 写行为和目标，不写情绪形容词。

给角色一个想要的东西 + 一个障碍，让情绪从冲突中自然产生。反应要提前发生，情绪有尾巴，手要一直有事做。

---

## 七、生产与迭代纪律

- 按场景块组织（每个场景一个文件夹 + shotlist）。
- 批量生成，场景内所有镜头共享同一份GEO和资产描述。
- 每次迭代只改一行，其余原样。
- 必须写日志：提示词版本、改了什么、结果。
- **10–15次法则**：超过就简化镜头（拆成两镜、减动作、改角度）。

### 从公开目录结构读出的额外专业习惯

| 目录现象 | 推论出的纪律 |
|----------|--------------|
| 大场景 `Scene 27` 拆成 `27.1/27.2/.../27.6` | 一个大场景按动作节点拆成更小的子镜头，每个子镜头单独处理 |
| `Scene 67` 再拆成 `67 / 67A / 67B` | 同一个大场景不同版本物理隔离，不混改 |
| `Scene 36,38,40` 放在一起 | 逻辑连续、风格统一的一组镜头可以放一起管理 |
| `Scene 25 Roko vs Robots` 单独命名 | 关键高潮戏份单独标记，方便重点迭代 |
| `Regenerations` 有14593个文件 | 接受AI需要多次试错，不指望一次生成完美 |
| `Flashbacks` 整个单独目录 | 闪回/幻想这些特殊色调片段统一分类，方便统一风格 |
| `Credits` 单独归档 | 参考来源、授权信息全留档，方便追溯 |

---

## 八、后期制作原则

- 清理阶段优先修脸和手。小缺陷逐帧修，大问题用最终提示词再生成一次。
- 调色先统一邻镜，再统一全片。调色逻辑其实已在地点资产里埋好。
- 声音尽量直接清理生成的唇同步音频，不重录。音效和音乐后期加。
- 连续环境音可以把画面轻微漂移的镜头"粘"在一起。

---

## 九、短视频完整可执行工作流（基于原原则缩放）

1. **文字大纲 + 拆镜表**（从1个角色 + 1–2个地点起步）。
2. **资产锁定**（角色三视图 + 正面无头 + 压力测试 + 声音/表演描述 + 地点3/4视图）。
3. **每场写GEO空间地图**。
4. **写分镜表**（每镜一个主动作，开头1秒定位置）。
5. **生成关键帧图片**（Hero Frame）。
6. **图片 + 完整骨架提示词 → 视频**（先720p测试，再上高分辨率）。
7. **严格迭代 + 日志**（一次改一行，超15次就简化）。
8. **剪辑 + 声音 + 调色**（SFX only生成，音乐后期加）。

---

## 十、常见失败点

- ❌ 资产没锁定就开生成 = 最大浪费
- ❌ 描述缩写 = 一致性立刻崩
- ❌ 给模型太多空间/动作 = 位置和表演失控
- ❌ 不写日志 = 好结果无法复现
- ❌ 坚持硬改提示词而不是简化镜头 = 烧点数无结果
- ❌ 生成时带音乐 = 后期剪辑灾难
- ❌ 正面全身图保留头 = 广角脸部质量崩
- ❌ 整图二次跑模型 = 皮肤死亡

---

## 十一、行动建议

Hell Grind 真正的价值不是"提示词有多长"，而是把AI视频从**"抽卡碰运气"**变成了**"可控工业化生产"**。

短视频创作者如果现在就把这套纪律内化（尤其是资产优先 + GEO空间锁定 + 一次改一行 + 超次简化），未来模型再强，你也会比别人更早拥有可复用的内容生产线。

原项目明确说：**这套系统不需要15人，一个人遵守规则就能跑起来。**

建议从最小可行项目开始：**1个角色 + 1个地点 + 8–12个镜头的1分钟短片**。把上面的五条规则和资产/GEO系统完整跑通一次，比看一百个教程都有效。

---

*From [ai-shortfilm-workflow](https://github.com/zengguangzhong/ai-shortfilm-workflow)*
